## Overview

`stampede` is a batch transcoding tool meant to be used for large media libraries. it performs transcoding jobs in batches

run `export STAMPEDE_CONFIG=<path-to-config>` to not have to specify `-c` each time you call `stampede` 